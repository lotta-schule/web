version: "3.7"

services:
  app:
    image: node:14
    volumes:
      - .:/usr/src/svc:delegated
      - /usr/src/app/node_modules
    working_dir: /usr/src/svc
    ports:
      - 3000:3000
      - 6006:6006
    command: bash -c "npm run dev"
    env_file: .env
    stdin_open: true
    tty: true
